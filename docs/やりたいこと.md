dify-context-and-json-orchestration.mdに書いてある内容を、以下の仕様に変更。

・sessionsというテーブルを作成
　bffがdifyを呼び出す際に、session_idを生成する。
　新規チャットでの初回呼び出しは、session_idをinputsに入れて投げる
　Dify内でgoal等を生成する場合は、従来、bffに返却していた情報をmcpサーバに送信する。その際、session_idをパラメータに入れる。
 session_idはfeに送り、feはこれを保管する
　feは２回目以降のコールでsession_idをbffに送り、bffは受け取ったsession_idを基に、goal_id等をsessionレコードから取得し、inputsに埋め込んでdifyを呼び出す