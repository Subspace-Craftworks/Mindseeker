# planning-api: list_subjects

## Purpose
List subjects, optionally filtered by goal.

## Parameters
- Optional:
  - `goal_id`
  - `status`
  - `priority`
  - `limit` (default `20`, min `1`, max `100`)

## Notes
- Use this to inspect goal breakdown.
