# planning-api: update_goal

## Purpose
Update an existing goal.

## Parameters
- Required:
  - `goal_id`
- Optional:
  - `title`
  - `description`
  - `status`

## Notes
- Use `status: inactive` to archive a goal manually if needed.
