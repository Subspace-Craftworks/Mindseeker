# planning-api: list_tasks

## Purpose
List tasks, optionally filtered by goal, subject, or issue.

## Parameters
- Optional:
  - `goal_id`
  - `subject_id`
  - `issue_id`
  - `status`
  - `limit` (default `20`, min `1`, max `100`)
