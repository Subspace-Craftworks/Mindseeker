# planning-api: list_events

## Purpose
List events with optional filters.

## Parameters
- Optional:
  - `goal_id`
  - `subject_id`
  - `issue_id`
  - `task_id`
  - `event_type`
  - `source`
  - `from`
  - `to`
  - `limit` (default `50`, min `1`, max `200`)
