# planning-api: create_task

## Purpose
Create a new task.

## Parameters
- Required:
  - `user_id`
  - `subject_id`
  - `title`
- Optional:
  - `issue_id`
  - `description`
  - `status` (default `todo`)
  - `due_date`
  - `assignee`
