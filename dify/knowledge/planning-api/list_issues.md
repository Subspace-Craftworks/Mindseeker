# planning-api: list_issues

## Purpose
List issues, optionally filtered by goal or subject.

## Parameters
- Optional:
  - `goal_id`
  - `subject_id`
  - `status`
  - `severity`
  - `limit` (default `20`, min `1`, max `100`)
